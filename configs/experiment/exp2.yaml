name: exp2

seed: 42

training:
  batch_size: 32
  epochs: 10

data_loading:
  # Defaults used by `python -m sign_ml.data --get-timing`
  num_workers: 0
  prefetch_factor: 2
  persistent_workers: true
  pin_memory: null
  multiprocessing_context: spawn
  batches_to_check: 32

optimizer:
  lr: 0.01
