device: auto
optimizer:
  _target_: torch.optim.Adam
paths:
  model_out: models/traffic_sign_model.pt
experiment:
  name: exp1
  seed: 42
  training:
    batch_size: 64
    epochs: 10
  optimizer:
    lr: 0.001
